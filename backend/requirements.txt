# Backend dependencies for SNCF Travel Assistant API
# Python 3.10+

# FastAPI framework
fastapi==0.109.0
uvicorn[standard]==0.27.0

# HTTP client for calling external APIs (Agent AI)
httpx==0.26.0

# Data validation
pydantic==2.5.3

# CORS middleware (included in FastAPI but explicit for clarity)
python-multipart==0.0.6

# Optional: for production deployment
gunicorn==21.2.0

# Optional: for environment variable management
python-dotenv==1.0.0
